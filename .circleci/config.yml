# Use the latest 2.1 version of CircleCI pipeline processing engine, see https://circleci.com/docs/2.0/configuration-reference/
version: 2.1

jobs:
  deploy-website:
    docker:
      # specify the version you desire here
      - image: circleci/node:8.11.1

    steps:
      - checkout
      - run:
          name: Deploying to GitHub Pages
          command: |
            git config --global user.email "antimatter15@users.noreply.github.com"
            git config --global user.name "Kevin Kwok"
            echo "machine github.com login antimatter15 password $GITHUB_TOKEN" > ~/.netrc
            cd website && yarn install && GIT_USER=antimatter15 yarn run publish-gh-pages

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - deploy-website:
        filters: *filter-only-master